clusterName: homelab-cluster
talosVersion: v1.12.0
kubernetesVersion: v1.35.0
endpoint: https://192.168.0.10:6443

# Disable Talos default CNI (Flannel) to use Cilium
cniConfig:
  name: none

# Allow scheduling workloads on control plane nodes for single node cluster
allowSchedulingOnControlPlanes: true

# Cluster-wide patches
patches:
  # Disable kube-proxy (Cilium handles this with kubeProxyReplacement=true)
  - |-
    cluster:
      proxy:
        disabled: true

nodes:
  - hostname: homelab-node-01
    ipAddress: 192.168.0.10
    controlPlane: true
    installDisk: /dev/nvme0n1
    # Image Factory Schematic: https://factory.talos.dev/?arch=amd64&bootloader=auto&cmdline-set=true&extensions=-&extensions=siderolabs%2Ftailscale&platform=metal&target=metal&version=1.12.0
    talosImageURL: factory.talos.dev/installer/4a0d65c669d46663f377e7161e50cfd570c401f26fd9e7bda34a0216b6f1922b
    patches:
      # Wipe disk on install
      - |-
        machine:
          install:
            wipe: true
      # Tailscale extension service configuration
      - |-
        apiVersion: v1alpha1
        kind: ExtensionServiceConfig
        name: tailscale
        environment:
          - TS_AUTHKEY=${TAILSCALE_AUTH_KEY}
