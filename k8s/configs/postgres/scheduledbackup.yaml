apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: postgres-cluster-daily
  namespace: postgres
spec:
  schedule: "0 0 21 * * *"  # Daily at 21:00 UTC (06:00 JST)
  backupOwnerReference: cluster
  cluster:
    name: postgres-cluster
