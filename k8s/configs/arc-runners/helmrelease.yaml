apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: homelab-k8s-runners
  namespace: arc-runners
spec:
  interval: 10m0s
  chart:
    spec:
      chart: gha-runner-scale-set
      sourceRef:
        kind: HelmRepository
        name: actions-runner-controller
        namespace: flux-system
      # Pinned; Renovate proposes upgrades (flux manager covers k8s/configs).
      # Keep in step with the gha-runner-scale-set-controller chart version.
      version: 0.14.2
  releaseName: homelab-k8s-runners
  install:
    createNamespace: false
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  values:
    githubConfigUrl: "https://github.com/aoshimash/homelab-k8s"
    githubConfigSecret: arc-github-app
    runnerScaleSetName: homelab-homelab-k8s
    minRunners: 0
    maxRunners: 3
    
    containerMode:
      type: kubernetes
      kubernetesModeWorkVolumeClaim:
        accessModes: ["ReadWriteOnce"]
        storageClassName: "longhorn"
        resources:
          requests:
            storage: 1Gi
    
    template:
      spec:
        securityContext:
          fsGroup: 1001
        containers:
        - name: runner
          image: ghcr.io/actions/actions-runner:2.337.0@sha256:e5496277be5d09bc968b3d64911b74e219ac4a3f2edce956a3ecf9271bea1ef4
          command: ["/home/runner/run.sh"]
          env:
          - name: ACTIONS_RUNNER_REQUIRE_JOB_CONTAINER
            value: "false"
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: optel-workout-runners
  namespace: arc-runners
spec:
  interval: 10m0s
  chart:
    spec:
      chart: gha-runner-scale-set
      sourceRef:
        kind: HelmRepository
        name: actions-runner-controller
        namespace: flux-system
      # Pinned; Renovate proposes upgrades (flux manager covers k8s/configs).
      # Keep in step with the gha-runner-scale-set-controller chart version.
      version: 0.14.2
  releaseName: optel-workout-runners
  install:
    createNamespace: false
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  values:
    githubConfigUrl: "https://github.com/aoshimash/optel-workout"
    githubConfigSecret: arc-github-app
    runnerScaleSetName: homelab-optel-workout
    minRunners: 0
    maxRunners: 3
    
    containerMode:
      type: kubernetes
      kubernetesModeWorkVolumeClaim:
        accessModes: ["ReadWriteOnce"]
        storageClassName: "longhorn"
        resources:
          requests:
            storage: 1Gi
    
    template:
      spec:
        securityContext:
          fsGroup: 1001
        containers:
        - name: runner
          image: ghcr.io/actions/actions-runner:2.337.0@sha256:e5496277be5d09bc968b3d64911b74e219ac4a3f2edce956a3ecf9271bea1ef4
          command: ["/home/runner/run.sh"]
          env:
          - name: ACTIONS_RUNNER_REQUIRE_JOB_CONTAINER
            value: "false"
