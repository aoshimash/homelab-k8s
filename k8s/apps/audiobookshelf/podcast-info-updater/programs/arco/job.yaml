# Manual Job for updating アルコ＆ピース D.C.GARAGE podcast metadata
# Run: kubectl apply then delete/recreate the job to run again
apiVersion: batch/v1
kind: Job
metadata:
  name: podcast-info-updater-arco
  namespace: audiobookshelf
spec:
  ttlSecondsAfterFinished: 3600
  template:
    spec:
      restartPolicy: Never
      securityContext:
        runAsNonRoot: true
        runAsUser: 99
        runAsGroup: 99
        fsGroup: 99
      containers:
        - name: podcast-info-updater
          image: ghcr.io/aoshimash/homelab-k8s/yt-dlp-rajiko:v1.0.0
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          command: ["/bin/sh", "/scripts/update-podcast-info.sh"]
          env:
            - name: PODCAST_ID
              value: "51491db5-5ba8-4e63-9f79-55d3c4257209"
            - name: PODCAST_DIR
              value: "/podcasts/arco"
            - name: AUDIOBOOKSHELF_API_KEY
              valueFrom:
                secretKeyRef:
                  name: radigo-audiobookshelf-api
                  key: api_key
            - name: AUDIOBOOKSHELF_BASE_URL
              valueFrom:
                secretKeyRef:
                  name: radigo-audiobookshelf-api
                  key: base_url
          volumeMounts:
            - name: scripts
              mountPath: /scripts
            - name: podcasts
              mountPath: /podcasts
              readOnly: true
      volumes:
        - name: scripts
          configMap:
            name: podcast-info-updater-script
            defaultMode: 0755
        - name: podcasts
          persistentVolumeClaim:
            claimName: audiobookshelf-podcasts
      imagePullSecrets:
        - name: ghcr-secret
