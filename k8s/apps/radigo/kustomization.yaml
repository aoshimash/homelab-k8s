apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Shared secrets
  - shared-secrets
  # Recording feature (radiko timefree recording)
  - recording
  # Metadata update feature (Audiobookshelf episode metadata)
  - metadata
