apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: immich
  namespace: immich
spec:
  interval: 10m0s
  chart:
    spec:
      chart: immich
      sourceRef:
        kind: HelmRepository
        name: immich-charts
        namespace: flux-system
      # Pinned; Renovate proposes upgrades (flux manager covers k8s/apps).
      version: 0.13.2
  releaseName: immich
  install:
    createNamespace: false
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  values:
    # Shared across all Immich components; DB_PASSWORD is the only sensitive
    # value, sourced from the SOPS-encrypted immich-db secret (must match the
    # CNPG immich role password provisioned in #259). Host/user/db-name are
    # non-secret and already public in k8s/configs/postgres. REDIS_HOSTNAME and
    # IMMICH_MACHINE_LEARNING_URL keep their chart defaults (immich-valkey /
    # immich-machine-learning) via Helm map merge.
    controllers:
      main:
        containers:
          main:
            env:
              DB_HOSTNAME: postgres-cluster-rw.postgres.svc
              DB_USERNAME: immich
              DB_DATABASE_NAME: immich
              DB_PASSWORD:
                valueFrom:
                  secretKeyRef:
                    name: immich-db
                    key: password
    # immich-server mounts the RWO immich-library volume and runs database
    # migrations at startup, so it must never overlap with its own previous
    # pod during an upgrade. The chart's own valkey controller already
    # defaults to Recreate; server does not. machine-learning is left on
    # RollingUpdate — its cache is an emptyDir and it holds no shared state.
    # Key path verified by rendering chart 0.13.2 (bjw-s common 5.0.1
    # `controllers.<name>.strategy`).
    server:
      controllers:
        main:
          strategy: Recreate
    immich:
      persistence:
        # The chart never creates the library volume; point it at our Longhorn PVC.
        library:
          existingClaim: immich-library
    # Bundled Redis-compatible cache (disabled by default); enabled instead of
    # running a separate Redis. Its job-queue volume stays ephemeral (emptyDir).
    valkey:
      enabled: true
