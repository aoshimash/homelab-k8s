apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - helmrepository.yaml
  - helmrelease.yaml
  - recurringjob.yaml
  - secret-r2-credentials.sops.yaml
  # Optional example secret to validate SOPS decryption wiring (non-sensitive demo value):
  # - examples/secret-demo.sops.yaml
