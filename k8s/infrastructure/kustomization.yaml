apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # CRDs first: PrometheusRule must exist before the rules below are applied.
  - prometheus-operator-crds/
  - cilium/
  - longhorn/
  - tailscale/
  - grafana-alloy/
  - kube-state-metrics/
  - metrics-server/
  - cloudnative-pg/
  - actions-runner-controller/
  - reloader/
